<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.awogni.carnetdenotes">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- ajout de la permission pour accéder au sd-->

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UeActivity" />
        <activity android:name=".ModuleActivity" />
        <activity android:name=".Modifier_Ue_Activity" />
        <activity android:name=".Modifier_Module_Activity" />
        <activity android:name=".Modification_Note_Activity" />
        <activity android:name=".AjoutNoteActivity" />
        <activity android:name=".CreerCarnetActivity" />
        <activity
            android:name=".ItemListActivity"
            android:label="@string/title_item_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MoyenneActivity"></activity>


    </application>

</manifest>